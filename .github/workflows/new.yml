name: Build and Deploy Api
on:
  push:
    branches:
      - main
jobs:
  api:
    name: Build and Deploy Api
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      run: ${GITHUB_REF#refs/heads/}
    
